# CodePrep Frontend

React/Vite frontend with a deterministic review mode and a lazy-loaded Clerk production mode.

## Commands

```bash
npm install
npm run dev
npm run check
```

Demo mode is the default. Set `VITE_CODEPREP_MODE=live` to load Clerk and call the backend. See the root [README](../README.md) for full setup.
